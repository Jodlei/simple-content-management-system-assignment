const NotFound = () => {
  return (
    <section>
      <h1
        style={{
          textAlign: "center",
        }}
      >
        We are sorry,
      </h1>
      <p> but the page you were looking for can’t be found..</p>
    </section>
  );
};

export default NotFound;
